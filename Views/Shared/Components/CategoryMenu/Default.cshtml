@model IEnumerable<PetCategory>

<li class="dropdown">
    <a asp-controller="Ad"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Категории<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Ad" asp-action="List"
                   asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="Ad" asp-action="List" asp-route-PetCategory="">Всички</a>
        </li>
    </ul>
</li>